# Zoom Video SDK Configuration
# Get these credentials from https://marketplace.zoom.us/develop/create

# SDK Key (Client ID from your Video SDK App)
VITE_ZOOM_SDK_KEY=your_sdk_key_here

# SDK Secret (Client Secret from your Video SDK App)
# IMPORTANT: Never expose this in client-side code in production
# This should only be used server-side to generate JWT tokens
VITE_ZOOM_SDK_SECRET=your_sdk_secret_here

# Optional: Backend API endpoint for token generation (production)
VITE_API_ENDPOINT=http://localhost:3001/api

# Optional: Session configuration
VITE_DEFAULT_SESSION_NAME=fitwithpari-session
VITE_SESSION_PASSWORD=Pass123!